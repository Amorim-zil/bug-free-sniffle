local OrionLib = loadstring(game:HttpGet("https://raw.githubusercontent.com/Amorim-zil/bug-free-sniffle/main/.gitignore"))()
local Window = OrionLib:MakeWindow({Name = "Anime Rarities Hub", HidePremium = false, SaveConfig = true, ConfigFolder = "AnimeRarities"})

local Tab = Window:MakeTab({
    Name = "Main",
    Icon = "rbxassetid://4483345998",
    PremiumOnly = false
})

Tab:AddButton({
    Name = "Fast Attack",
    Callback = function()
        local player = game.Players.LocalPlayer
        local character = player.Character or player.CharacterAdded:Wait()
        local humanoid = character:FindFirstChildOfClass("Humanoid")
        if humanoid then
            -- Assume there's a method to set attack speed in the game's code
            humanoid.WalkSpeed = 100 -- Adjust speed as needed
        end
    end
})

Tab:AddButton({
    Name = "Increase Damage",
    Callback = function()
        local player = game.Players.LocalPlayer
        -- Modify the code to adjust damage
        local character = player.Character or player.CharacterAdded:Wait()
        local humanoid = character:FindFirstChildOfClass("Humanoid")
        if humanoid then
            -- Example: Increase damage by changing a property or calling a method
            -- Replace with actual method or property to increase damage
            humanoid.Health = humanoid.Health + 1000 -- Example adjustment
        end
    end
})

Tab:AddButton({
    Name = "Increase Luck",
    Callback = function()
        local player = game.Players.LocalPlayer
        -- Modify the code to adjust luck
        local character = player.Character or player.CharacterAdded:Wait()
        if character then
            -- Example: Increase luck by modifying a player property or calling a method
            -- Replace with actual method or property to increase luck
            local stats = player:FindFirstChild("Stats")
            if stats then
                stats.Luck.Value = stats.Luck.Value + 10 -- Example adjustment
            end
        end
    end
})

Tab:AddButton({
    Name = "Aura Kill",
    Callback = function()
        local player = game.Players.LocalPlayer
        local character = player.Character or player.CharacterAdded:Wait()
        local position = character.HumanoidRootPart.Position
        for _, monster in ipairs(workspace.Monsters:GetChildren()) do
            if (monster.PrimaryPart.Position - position).magnitude < 10 then -- Adjust radius as needed
                -- Example: Destroy or defeat monsters within range
                monster:Destroy() -- Example action
            end
        end
    end
})

OrionLib:Init()
